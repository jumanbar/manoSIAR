% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/informes.R
\name{g_mes}
\alias{g_mes}
\alias{g_mes_all}
\title{Graficos de parámetros por mes}
\usage{
g_mes(
  .data,
  id_parametro = NULL,
  pos_leyenda = "none",
  nombre_clave_param,
  ylab = NULL,
  t_eti
)

g_mes_all(.data, id_parametro, t_eti, ...)
}
\arguments{
\item{.data}{data.frame. Datos del SIA}

\item{id_parametro}{integer. Id de uno/escalar (\code{g_mes}) o varios elementos
(\code{g_mes_all}). Se debe(n) corresponder con los ids encontrados columna
homónima de \code{\link{sia_parametro}}}

\item{pos_leyenda}{character. Valor enviado directamente al argumento
\code{legend.position} de la función \code{\link[ggplot2]{theme}}}

\item{nombre_clave_param}{character. Opcional. Nombre clave del parámetro
(ej.: "PT"). Se corresponde con la columna \code{nombre_clave} de
\code{\link{sia_parametro}}}

\item{ylab}{Opcional. Etiqueta para el eje y del gráfico resultante. Si este
argumento no es especificado, se utilizará la función \code{eti} para crear
la etiqueta.}

\item{t_eti}{\code{tbl_df}. Opcional. Tabla de etiquetas}

\item{...}{Argumentos adicionales para pasar a
\code{\link[patchwork]{wrap_plots}}}
}
\value{
Objeto de clase \code{gg} (hereda de \code{\link[ggplot2]{ggplot}}).


}
\description{
\code{g_mes} grafica los valores de un parámetro por mes. \code{g_mes_all} en escencia
llama a \code{\link{g_mes}} tantas veces como id de parámetros en
\code{id_parametro}, realizando una figura única que combina los gráficos de los
parámetros individuales.
}
\details{
Requiere de la tabla decreto para agregar las líneas horizontales
correspondientes a los valores límites aceptados para la clase 1 de aguas
(decreto 253/79).
}
\section{Functions}{
\itemize{
\item \code{g_mes_all}: Varios gráficos de valores de parámetros por mes en una
misma figura
}}

\examples{
p <- c(PT=2098, NT=2102, ST=2028, Conduc=2009)
dfo <- data.frame(mes = rep(3:6, 4),
                  codigo_pto = rep(c("RN1", "RN2", "RN3", "RN5"), each = 4),
                  id_parametro = rep(p, each = 4*4),
                  valor = c(rnorm(4*4, mean = 30, sd = 5),
                            rnorm(4*4, mean = 55, sd = 15),
                            rnorm(4*4, mean = 315, sd = 50),
                            rnorm(4*4, mean = 500, sd = 80)))
dfo$valor[sample(4*4*4, 5)] <- NA
dplyr::filter(dfo, is.na(valor))
g_mes(dfo, id_parametro = 2009, ylab = 'Cond (Î¼S/cm)')
g_mes(dfo, nombre_clave_param = 'Conduc', ylab = 'Cond (Î¼S/cm)')
g_mes_all(dfo, id_parametro = p)
g_mes_all(dfo, id_parametro = 2098L)
g_mes_all(dfo, id_parametro = 2008L) # VacÃ­o
g_mes_all(dfo, id_parametro = c(p, 2008L)) # VacÃ­o
}
\seealso{
\code{\link{t_eti_base}}
}
